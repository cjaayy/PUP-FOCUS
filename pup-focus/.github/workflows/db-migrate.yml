name: Database Migrate

on:
  workflow_dispatch:

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Apply SQL files in infrastructure/database/migrations using Supabase CLI in secured pipeline."
